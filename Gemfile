source 'https://rubygems.org'

# Pin ffi for Ruby 2.6 compatibility (ffi >= 1.17 requires Ruby 3.0+)
gem 'ffi', '~> 1.15.5'

group :jekyll_plugins do
  gem 'jekyll'
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  # gem 'jemoji'  # Disabled: requires nokogiri native extensions that conflict with Ruby 2.6
  gem 'webrick', '~> 1.8'
end

gem 'github-pages'
